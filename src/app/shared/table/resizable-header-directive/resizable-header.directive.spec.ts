import { ResizableHeaderDirective } from './resizable-header.directive';

describe('ResizableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
